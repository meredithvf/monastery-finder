/** Neutral imputation when a feature is missing or unparseable. */
export const NEUTRAL_SCORE = 0.5 as const;

/** User spectrum inputs are stored on a 0–100 scale. */
export const USER_SPECTRUM_MAX = 100;

/** Readiness seriousness is stored on a 1–5 scale. */
export const READINESS_SERIOUSNESS_MIN = 1;
export const READINESS_SERIOUSNESS_MAX = 5;
